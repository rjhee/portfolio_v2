import React from 'react';

const Contact = (props) => {
    const { isOpen } = props;
    return (
        <div className='screen contact '>
            <h1>Contact</h1>
        </div>
    );
};

export default Contact;
